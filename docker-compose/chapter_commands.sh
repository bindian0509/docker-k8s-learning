docker-compose up 
docker-compose up -d 


docker-compose down 
# to remove volumes as well 
docker-compose down -v 

# force build 
docker-compose up --build

# only build no container 
docker-compose build 
