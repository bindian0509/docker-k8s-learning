# to start laravel project 
docker-compose run --rm composer create-project --prefer-dist laravel/laravel .

# to run the laravel app (with depends) 
docker-compose up --build server                                                                                                                                                              